// let/var = variable = doos met inhoud
// array = lijst van variable = ladenkast met dozen

let eindGetal = parseInt(prompt("Voer een getal in:"));
let resultaat = 1;

const array = [];
for (let i = 1 ; resultaat < eindGetal; i++) {
    array.push(som);
    som += i;
}

console.log(array.join());
